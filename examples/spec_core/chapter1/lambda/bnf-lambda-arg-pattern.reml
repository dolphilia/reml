module Spec.Core.Chapter1.Lambda.ArgPattern

use Core.Prelude

// BNF: Lambda（docs/spec/1-5-formal-grammar-bnf.md §4）
let destruct_pair = |(label, count)| label + count

fn main() -> Int {
  destruct_pair((3, 4))
}
