module Spec.Core.Chapter2.Parser.UnicodeGraphemeColumn

use Core.Prelude

// 目的: Unicode（ZWJ 絵文字など）を含む行で、エラー位置の列（column）が
// `Core.Text` のグラフェム基準（拡張書記素クラスタ）と整合することを回帰として固定する（CP-WS5-002）。
//
// 期待: 失敗箇所は `)`（右項欠落）で、highlight と location.column が崩れない。

fn main() -> Int {
  ("👩‍💻" +)
}

