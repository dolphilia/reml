module Spec.Core.Chapter2.Parser.CutUnclosedParen

use Core.Prelude

// 目的: 括弧ペアの未完（開き括弧に対して閉じ括弧が無い）を、回帰ケースとして固定する。
// 想定する改善: `(` を消費した時点で分岐が確定し、入力終端（または `}`）で
// 「`)` が必要」と自然に収束する診断を提示できる（`cut` による期待集合の再初期化）。

fn main() -> Int {
  (1 + 2
}

