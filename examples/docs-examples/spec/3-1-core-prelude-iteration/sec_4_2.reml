fn __rustcap_guard__(unit: Int) = unit

type Map<K, V> = ()
type Range<T> = { start: T, end: T }
type Histogram = ()
type Result<T, E> = ()

enum HistogramError = OutOfRange(f64)

struct HistogramCollector {
  buckets: Map<Range<f64>, u32>,
}

fn new_histogram_collector() -> HistogramCollector = todo
