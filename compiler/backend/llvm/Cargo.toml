[package]
name = "reml-llvm-backend"
version = "0.1.0"
edition = "2021"
description = "Rust LLVM バックエンドの TargetMachine/TypeMapping/FFI ロワリングのスケルトン"
license = "Apache-2.0 OR MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
native-unstable = []
