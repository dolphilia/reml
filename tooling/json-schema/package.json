{
  "name": "reml-json-schema",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Schema validation utilities for Reml audit and diagnostic flows",
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-cli": "^5.0.0"
  }
}
