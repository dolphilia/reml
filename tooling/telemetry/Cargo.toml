[package]
name = "reml-telemetry-tools"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "export_graphviz"
path = "export_graphviz.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
