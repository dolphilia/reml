# Phase 4 spec_core 失敗ログ

- 生成時刻: 2025-12-08 20:00:34Z
- レポート: /Users/dolphilia/github/kestrel/reports/spec-audit/ch5/spec-core-dashboard.md
- 件数: 44 件 / 成功 0 件 / 失敗 44 件

## 失敗シナリオ詳細

### CH1-LET-001

- ファイル: `examples/spec_core/chapter1/let_binding/bnf-valdecl-let-simple-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: 基本的な let 束縛。Result/Option を含まない純粋ケースで、Phase4TestCase における基準入力。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-LET-002

- ファイル: `examples/spec_core/chapter1/let_binding/bnf-valdecl-let-pattern-tuple.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: タプルパターン束縛。仕様上は許容だが OCaml 実装では過去に制約があったため検証対象。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-LET-003

- ファイル: `examples/spec_core/chapter1/let_binding/bnf-valdecl-let-shadow-unicode.reml`
- 期待 Diagnostics: `language.shadowing.unicode`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: Unicode 識別子とシャドーイング境界。`αβ` のような識別子を1回だけ再束縛する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-MATCH-001

- ファイル: `examples/spec_core/chapter1/match_expr/bnf-matchexpr-option-canonical.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `Option` に対する `match ... with` の正準例。`Some`/`None` 分岐で BNF の最小形を固定。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-MATCH-002

- ファイル: `examples/spec_core/chapter1/match_expr/bnf-matchexpr-tuple-alternate.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: TuplePattern と `_` を組み合わせた `match` バリエーション。分岐順序とフォールバック処理を検証。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-EFFECT-004

- ファイル: `examples/spec_core/chapter1/effect_handlers/bnf-handleexpr-missing-with.reml`
- 期待 Diagnostics: `effects.handler.missing_with`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `with` 節を省略する意地悪ケース。診断キーとメッセージを固定化。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-EFF-006

- ファイル: `examples/spec_core/chapter1/effect_handlers/bnf-handleexpr-perform-counter.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `perform` と `handle ... with handler` を同時に提示する効果ハンドラの派生記法。`resume` を介した戻り値の合成を確認。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-MOD-003

- ファイル: `examples/spec_core/chapter1/module_use/bnf-compilationunit-module-use-alias-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `module spec_core.match_guard` と再帰 `use` を同一 `.reml` で実行し Prelude 名寄せのゴールデンを作成する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-MOD-004

- ファイル: `examples/spec_core/chapter1/module_use/bnf-usedecl-super-root-invalid.reml`
- 期待 Diagnostics: `language.use.invalid_super`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: ルート直下で `super` を参照した際の拒否診断を明示し Chapter1 §B.1 の禁止事項をテスト化する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-ATTR-101

- ファイル: `examples/spec_core/chapter1/attributes/bnf-attr-cfg-let-gate-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `@cfg(target = \"cli\")` で `let` ブロックが有効化される既定経路を `RunConfig` と連携して検証する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-ATTR-102

- ファイル: `examples/spec_core/chapter1/attributes/bnf-attr-cfg-missing-flag-error.reml`
- 期待 Diagnostics: `language.cfg.unsatisfied_branch`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: 未定義ターゲットを指定した `@cfg` が `language.cfg.unsatisfied_branch` を返すことを Chapter1 §B.6 準拠で確認する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-FN-101

- ファイル: `examples/spec_core/chapter1/fn_decl/bnf-fndecl-generic-default-effect-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: ジェネリック/デフォルト引数/効果注釈を組み合わせた `fn` 宣言を Chapter1 §B.4 の要件通り通過させる。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-TYPE-201

- ファイル: `examples/spec_core/chapter1/type_decl/bnf-typedef-sum-recordpattern-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: SumType と Record パターンの束縛を同一 `.reml` にまとめ BNF 通り受理されることを確認する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-TRAIT-301

- ファイル: `examples/spec_core/chapter1/trait_impl/bnf-traitdecl-default-where-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `trait Show<T> where T: Copy` とデフォルトメソッドの辞書生成ログを Chapter1 §B.1 の通り固定する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-IMPL-302

- ファイル: `examples/spec_core/chapter1/trait_impl/bnf-impldecl-duplicate-error.reml`
- 期待 Diagnostics: `typeclass.impl.duplicate`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: 同一型への重複 `impl` を禁止する診断を B.2 の整合性規則に沿ってゴールデン化する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-INF-601

- ファイル: `examples/spec_core/chapter1/type_inference/bnf-inference-let-generalization-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `let id = fn x => x` が多相化され `Vec<i64>` と `Vec<Text>` で共有できることを Chapter1 §H.1 に基づき確認する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-INF-602

- ファイル: `examples/spec_core/chapter1/type_inference/bnf-inference-value-restriction-error.reml`
- 期待 Diagnostics: `language.inference.value_restriction`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `var cell = []` の一般化拒否を `language.inference.value_restriction` 診断で再現し C.3 の値制限をテストする。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-EFF-701

- ファイル: `examples/spec_core/chapter1/effects/bnf-attr-pure-perform-error.reml`
- 期待 Diagnostics: `effects.purity.violated`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `@pure fn` 内で `perform Console.log` を呼び出した際の純粋性違反を Chapter1 §B と Stage 要件に沿って診断する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-DSL-801

- ファイル: `examples/spec_core/chapter1/conductor/bnf-conductor-basic-pipeline-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `conductor telemetry { channels { ... } execution { ... } }` の DSL 制御ブロックを Chapter1 §B.8 に沿って再現し監査タグ連携を確認する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-MATCH-003

- ファイル: `examples/spec_core/chapter1/match_expr/bnf-matchexpr-when-guard-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `Some(x) when x > 10 as large` のような `when`/`as` パターンを Chapter1 §C.3 通り受理することを確認する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH2-PARSE-101

- ファイル: `examples/spec_core/chapter2/parser_core/core-parse-or-commit-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `Core.Parse.or` + `commit` の組合せで Backtracking コストを制御する基準ケースを作成する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH2-PARSE-201

- ファイル: `examples/spec_core/chapter2/parser_core/core-parse-recover-diagnostic.reml`
- 期待 Diagnostics: `core.parse.recover.branch`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `Parse.recover` が `core.parse.recover.branch` 診断を生成し Diagnostics chapter と同期することを確認する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH2-STREAM-301

- ファイル: `examples/spec_core/chapter2/streaming/core-parse-runstream-demandhint-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `run_stream` と `DemandHint::More` の協調を示す Streaming API 基準ケースを chapter2 §C-1 に基づき追加する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH2-OP-401

- ファイル: `examples/spec_core/chapter2/op_builder/core-opbuilder-level-conflict-error.reml`
- 期待 Diagnostics: `core.parse.opbuilder.level_conflict`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: 同一レベルへ異なる fixity を登録した際の `core.parse.opbuilder.level_conflict` 診断をゴールデン化し優先度規則を確認する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-LET-004

- ファイル: `examples/spec_core/chapter1/let_binding/bnf-valdecl-missing-initializer-error.reml`
- 期待 Diagnostics: `parser.syntax.expected_tokens`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: 初期化式を欠落させ parser.syntax.expected_tokens を誘発する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-MATCH-004

- ファイル: `examples/spec_core/chapter1/match_expr/bnf-matchexpr-missing-arrow-error.reml`
- 期待 Diagnostics: `parser.syntax.expected_tokens`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `->` 欠落により構文エラーを明示する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-BLOCK-001

- ファイル: `examples/spec_core/chapter1/block/bnf-block-unclosed-brace-error.reml`
- 期待 Diagnostics: `parser.syntax.expected_tokens`
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `}` が無いブロック終端エラーを再現する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-IF-101

- ファイル: `examples/spec_core/chapter1/control_flow/bnf-ifexpr-blocks-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: if-then-else でブロック値が返る基本例。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-IF-102

- ファイル: `examples/spec_core/chapter1/control_flow/bnf-ifexpr-missing-else-type-mismatch.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: else 省略で非 Unit を返そうとした型不一致の境界例。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-LOOP-101

- ファイル: `examples/spec_core/chapter1/control_flow/bnf-loopexpr-break-value-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: loop から break 値を返す最小例。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-LOOP-102

- ファイル: `examples/spec_core/chapter1/control_flow/bnf-loopexpr-unreachable-code.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: break 後に到達不能な式が残る診断例。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-WHILE-101

- ファイル: `examples/spec_core/chapter1/control_flow/bnf-whileexpr-condition-bool-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: Bool 条件で while を評価するケース。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-WHILE-102

- ファイル: `examples/spec_core/chapter1/control_flow/bnf-whileexpr-condition-type-error.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: Bool 以外を条件に使った拒否診断。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-FOR-101

- ファイル: `examples/spec_core/chapter1/control_flow/bnf-forexpr-iterator-pattern-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: Pattern でイテレータ要素を分解する for 式。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-FOR-102

- ファイル: `examples/spec_core/chapter1/control_flow/bnf-forexpr-iterator-invalid-type.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: 非イテレータ値を `for` に渡す診断例。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-LIT-201

- ファイル: `examples/spec_core/chapter1/literals/bnf-literal-int-boundary-max.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: i64 最大値を下線区切りで表す境界テスト。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-LIT-202

- ファイル: `examples/spec_core/chapter1/literals/bnf-literal-float-forms.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: 指数表記や大文字 E を含む浮動小数リテラル例。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-LIT-203

- ファイル: `examples/spec_core/chapter1/literals/bnf-literal-string-raw-multiline.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: raw 文字列と複数行文字列を並記する。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-TYPE-202

- ファイル: `examples/spec_core/chapter1/type_decl/bnf-typedecl-alias-generic-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: ジェネリック alias を入れ子で定義する例。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-TYPE-203

- ファイル: `examples/spec_core/chapter1/type_decl/bnf-typedecl-new-struct-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: `type Name = new { ... }` の構造体ラッパを示す。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-FN-102

- ファイル: `examples/spec_core/chapter1/fn_decl/bnf-fndecl-no-args-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: 引数無し関数の宣言と呼び出しを示す。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-FN-103

- ファイル: `examples/spec_core/chapter1/fn_decl/bnf-fndecl-return-inference-error.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: 戻り値注釈無しで分岐戻り型が衝突する推論エラー。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-LAMBDA-101

- ファイル: `examples/spec_core/chapter1/lambda/bnf-lambda-closure-capture-ok.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: 外側変数を捕捉するラムダを示す。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```

### CH1-LAMBDA-102

- ファイル: `examples/spec_core/chapter1/lambda/bnf-lambda-arg-pattern.reml`
- 期待 Diagnostics: —
- 実際 Diagnostics: —
- Exit code: 101
- 備考: パターン引数を用いたラムダの短縮形。
- エラー: CLI から JSON 出力が得られませんでした

#### stdout
```
(empty)
```

#### stderr
```
error: current package believes it's in a workspace when it's not:
current:   /Users/dolphilia/github/kestrel/compiler/rust/frontend/Cargo.toml
workspace: /Users/dolphilia/github/kestrel/Cargo.toml

this may be fixable by adding `compiler/rust/frontend` to the `workspace.members` array of the manifest located at: /Users/dolphilia/github/kestrel/Cargo.toml
Alternatively, to keep it out of the workspace, add the package to the `workspace.exclude` array, or add an empty `[workspace]` table to the package's manifest.
```
