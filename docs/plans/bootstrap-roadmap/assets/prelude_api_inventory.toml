# Reml Core Prelude API インベントリ（雛形）
# - 参照仕様: docs/spec/3-1-core-prelude-iteration.md
# - 目的: Option/Result/Iter API の実装状況を機械可読に追跡する
# - スキーマ: v0 (WBS 2.1a 時点の暫定)

[meta]
schema = "core_prelude_api_inventory/v0"
spec = "docs/spec/3-1-core-prelude-iteration.md"
last_updated = "2025-12-24 / WBS 3.1c-F2"
maintainer = "Core Library Team"

[[api]]
module = "Option"
name = "is_some"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "not_applicable"
notes = "`core_prelude_option_result.rs` の `option_is_some` で bool 判定を固定"
wbs = "2.1b"

[[api]]
module = "Option"
name = "map"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "not_applicable"
notes = "`core_prelude_option_result.rs` の `option_map` スナップショットで 2 ケース検証"
wbs = "2.1b"

[[api]]
module = "Option"
name = "and_then"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "not_applicable"
notes = "連鎖挙動は `option_and_then` ラインと `Try` 実装で検証"
wbs = "2.1b"

[[api]]
module = "Option"
name = "expect"
effect = "effect {debug}"
stage = "formal"
rust_status = "implemented"
ocaml_status = "runtime-only"
notes = "`panic_option_expect` で `effect {debug}` 専用 panic を分離し `option_expect` で確認"
wbs = "2.1b"

[[api]]
module = "Option"
name = "ok_or"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "not_applicable"
notes = "`Result` 化は `option_ok_or` と `option_result_bridge` シナリオで固定"
wbs = "2.1b"

[[api]]
module = "Option"
name = "unwrap_or"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "not_applicable"
notes = "既定値挙動は `option_unwrap_or` スナップショットで監視"
wbs = "2.1b"

[[api]]
module = "Result"
name = "map"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "`result_map` 行で Ok/Err 両ケースの戻り値を固定"
wbs = "2.1b"

[[api]]
module = "Result"
name = "map_err"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "エラー変換は `result_map_err` で `String` 化を確認"
wbs = "2.1b"

[[api]]
module = "Result"
name = "and_then"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "`result_and_then` シナリオでチェイン挙動を記録"
wbs = "2.1b"

[[api]]
module = "Result"
name = "or_else"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "回復経路は `result_or_else` で Ok/Err 両方を比較"
wbs = "2.1b"

[[api]]
module = "Result"
name = "unwrap_or"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "既定値の受け渡しは `result_unwrap_or` で固定"
wbs = "2.1b"

[[api]]
module = "Result"
name = "expect"
effect = "effect {debug}"
stage = "formal"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "panic 経路は `panic_result_expect` を分離し `result_expect` で値ケースを監視"
wbs = "2.1b"

[[api]]
module = "Result"
name = "to_option"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "Option への射影は `result_to_option` 行で CI 監視"
wbs = "2.1b"

[[api]]
module = "Result"
name = "from_option"
effect = "@pure"
stage = "formal"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "Option からの持ち上げは `result_from_option` で Ok/Err 双方を固定"
wbs = "2.1b"

[[api]]
module = "Result"
name = "ensure"
effect = "@pure"
stage = "draft"
rust_status = "pending"
ocaml_status = "not_available"
notes = "`docs/spec/3-1-core-prelude-iteration.md` の Guard 節に基づき設計"
wbs = "2.2a"

[[api]]
module = "Iter"
name = "from_list"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F1-2: `core_iter_generators.rs::from_list_roundtrip` で `ListCollector` ノードを再利用し `CollectOutcome` の Stage/Effect を転写しつつ `EffectLabels::residual = []` を維持して `@pure` を保証した。`collect-iterator-audit --section iter --case from_list` の KPI を `reports/spec-audit/ch1/iter.json#audit_cases.from_list` に記録し `reports/spec-audit/ch0/links.md#iter-generators` に証跡を残した。"
wbs = "3.1c F1"

[[api]]
module = "Iter"
name = "from_result"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F1-2: `core_iter_generators.rs::from_result_passthrough` で `Result<T, E>` を `Iter<T>` に昇格させ `CollectOutcome` の `stage`/`effect` を `ListCollector` と一致させ、`collect-iterator-audit --section iter --case from_result` の JSON を `reports/spec-audit/ch1/iter.json#audit_cases.from_result` へ追加した。"
wbs = "3.1c F1"

[[api]]
module = "Iter"
name = "from_fn"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F1-2: `core_iter_generators.rs::from_fn_counter` で `FnMut() -> Option<T>` を `IterSeed` に包み `EffectLabels::residual = []` として `@pure` を維持し、`collect-iterator-audit --section iter --case from_fn` で `iterator.stage.audit_pass_rate=1.0`/`collector.effect.mem=0` の KPI を `reports/spec-audit/ch1/iter.json#audit_cases.from_fn` へ連携した。"
wbs = "3.1c F1"

[[api]]
module = "Iter"
name = "empty"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F1-3: `Iter::empty` を ZST 共有で実装し、`core_iter_generators.rs::empty_collect` の snapshot で `iterator.stage.audit_pass_rate=1.0`・`EffectLabels::residual=[]` を確認。`collect-iterator-audit --section iter --case empty` の `iterator.empty.items=0` KPI を `reports/spec-audit/ch1/iter.json#audit_cases.empty` へ記録し、`reports/spec-audit/ch0/links.md#iter-generators` にコマンドを追記。"
wbs = "3.1c F1-3"

[[api]]
module = "Iter"
name = "once"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F1-3: `Iter::once` を `IterSeed::singleton` で共通化し `core_iter_generators.rs::once_collect`（`repeat_take` 内包）で 1 要素のみを保証。`collect-iterator-audit --section iter --case once` が `iterator.stage.audit_pass_rate=1.0`・`iterator.once.length=1` を記録。"
wbs = "3.1c F1-3"

[[api]]
module = "Iter"
name = "repeat"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F1-3: `Iter::repeat` で `diagnostic.extensions[\"iterator.repeat\"] = true` を注入し、`core_iter_generators.rs::repeat_take` が `collect-iterator-audit --section iter --case repeat` の `iterator.repeat.flagged=true` KPI を `reports/spec-audit/ch1/iter.json#audit_cases.repeat` へ残す構成を確立。"
wbs = "3.1c F1-3"

[[api]]
module = "Iter"
name = "range"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F1-3: `RangeState`/`IterRangeError` を `iter/errors.rs` へ切り出し `Iter::range` を `effect=@pure` のまま実装。`core_iter_generators.rs::range_basic`/`range_overflow_guard` が `iterator.range.overflow_guard=1` を `collect-iterator-audit --section iter --case range` 経由で `reports/spec-audit/ch1/iter.json#audit_cases.range` に保存。"
wbs = "3.1c F1-3"

[[api]]
module = "Iter"
name = "unfold"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F1-4: `core_iter_generators.rs::unfold_fibonacci_pipeline` で `ListCollector` と往復し、`collect-iterator-audit --section iter --case unfold` に `iterator.stage.audit_pass_rate=1.0` と `iterator.unfold.depth=8` を記録。`cargo xtask prelude-audit --section iter --wbs 3.1c-F1-4` の結果を `reports/spec-audit/ch1/iter.json#audit_cases.unfold` へ連携済み。"
wbs = "3.1c F1-4"

[[api]]
module = "Iter"
name = "try_unfold"
effect = "effect {debug}"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F1-4: `core_iter_generators.rs::try_unfold_error_passthrough` で `Result<Option<(T, State)>, E>` の `Err(E)` が `iterator.error.kind=\"try_unfold\"` として監査ログへ記録されることを snapshot 化。`collect-iterator-audit --section iter --case try_unfold` の KPI（`iterator.stage.audit_pass_rate=1.0`, `iterator.try_unfold.error_kind=\"try_unfold\"`, `EffectLabels::debug=true`）を `reports/spec-audit/ch1/iter.json#audit_cases.try_unfold` に保存。"
wbs = "3.1c F1-4"

[[api]]
module = "Iter"
name = "map"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F2: `compiler/rust/runtime/src/prelude/iter/adapters.rs` に `Iter::map` を追加し、`compiler/rust/frontend/tests/core_iter_adapters.rs::iter_map_filter_pipeline` で `ListCollector` ブリッジを通さずに @pure パイプラインを固定。`iter_buffered_sets_mem_effects` 実行ログは `reports/spec-audit/ch1/iter.json#audit_cases.map` へ転記。"
wbs = "3.1c F2"

[[api]]
module = "Iter"
name = "filter_map"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F2: `filter_map` を `iter/adapters.rs` に実装し、`core_iter_adapters.rs::iter_filter_map_skips_invalid` が JSON 変換の成功/失敗を snapshot で再現。`collect-iterator-audit --section iter --case filter_map` の結果を `reports/spec-audit/ch1/iter.json#audit_cases.filter_map` に追加。"
wbs = "3.1c F2"

[[api]]
module = "Iter"
name = "flat_map"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F2: `core_iter_adapters.rs::iter_flat_map_expands_sequences` で `Iter::flat_map` の Stage/効果タグを固定し、`reports/spec-audit/ch1/iter.json` 内の `iterator.stage.audit_pass_rate` を更新。Rust 実装は `iter/adapters.rs` に格納。"
wbs = "3.1c F2"

[[api]]
module = "Iter"
name = "filter"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F2: `Iter::filter` を `iter/adapters.rs` に追加し、`core_iter_adapters.rs::iter_map_filter_pipeline` で `map→filter` のパイプラインを固定。`collect-iterator-audit --section iter --case filter` を `reports/spec-audit/ch1/iter.json#audit_cases.filter` へ保存。"
wbs = "3.1c F2"

[[api]]
module = "Iter"
name = "scan"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F2: `iter_scan_tracks_running_totals` で `Iter::scan` の状態遷移と Stage を監視し、`docs/notes/core-library-outline.md` へ実装ログを追記。Rust 実装は `iter/adapters.rs::scan`。"
wbs = "3.1c F2"

[[api]]
module = "Iter"
name = "take"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F2: `iter_take_drop_enumerate` テストで `drop→take` の組合せ利用を固定し、`collect-iterator-audit --section iter --case take` の結果を `reports/spec-audit/ch1/iter.json#audit_cases.take` へ追加。"
wbs = "3.1c F2"

[[api]]
module = "Iter"
name = "drop"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F2: `iter_take_drop_enumerate` で `Iter::drop` のスキップ挙動を検証し、`iter/adapters.rs` の `drop` 実装を Stage 監査ログに紐付け。"
wbs = "3.1c F2"

[[api]]
module = "Iter"
name = "enumerate"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F2: `iter_take_drop_enumerate` で `enumerate` のインデックス付けを固定し、`reports/spec-audit/ch1/iter.json#audit_cases.enumerate` へ KPI を追記。"
wbs = "3.1c F2"

[[api]]
module = "Iter"
name = "zip"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F2: `core_iter_adapters.rs::iter_zip_pairs_sequences` で 2 つの `Iter` を同期結合し、効果タグの論理和を `EffectSet::union` で記録。`reports/spec-audit/ch1/iter.json#audit_cases.zip` に結果を保存。"
wbs = "3.1c F2"

[[api]]
module = "Iter"
name = "buffered"
effect = "effect {mem}"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F2: `BufferStrategy::{DropOldest,Grow}` を `iter/adapters.rs::buffered` に実装し、`IterStep::Error(IterError::Buffer)` で容量超過を保護。`core_iter_adapters.rs::iter_buffered_sets_mem_effects` が `effect_labels.mem=true` と `mem_bytes=capacity` を監視し、`reports/spec-audit/ch1/iter.json#audit_cases.buffered`/`docs/plans/bootstrap-roadmap/0-3-audit-and-metrics.md` に KPI を追記。"
wbs = "3.1c F2"

[[api]]
module = "Iter"
name = "try_fold"
effect = "@pure"
stage = "beta"
rust_status = "working"
ocaml_status = "typeck-only"
notes = "`iter_try_fold_diag` で `typeclass.iterator.stage_mismatch` が発生しないことを `reports/spec-audit/ch1/iter.json` へ記録"
wbs = "3.1a"

[[api]]
module = "Iter"
name = "try_collect"
effect = "@pure"
stage = "beta"
rust_status = "working"
ocaml_status = "typeck-only"
notes = "`Iter::try_collect(SetCollector)` と `collect-iterator-audit-metrics.py --module iter --section collectors --case iter-f3` による `collector.effect.mem`/`iterator.stage.audit_pass_rate` KPI を `reports/spec-audit/ch1/iter.json` に記録"
wbs = "3.1a"

[[api]]
module = "Iter"
name = "module.structure"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "typeck-only"
notes = "F1-1: `compiler/rust/runtime/src/prelude/iter/generators.rs` に `Iter` の `from_state`/`stage_snapshot`/`effect_labels` を移管し `IterStepMetadata`/`attach_effects` を追加、`docs-migrations.log` + `module = \"Iter\"` の `prelude_api_inventory.toml` 行へ `WBS 3.1c-F1` 脚注を記録して進捗を可視化"
wbs = "3.1c F1"

# F2-4: `cargo xtask prelude-audit --section iter --filter collector` でコレクタ API 在庫を再検証し、Rust 実装側を `implemented` と判定

[[api]]
module = "Collector"
name = "trait.new"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "not_available"
notes = "F2: `ListCollector`/`MapCollector` 実装が `Collector::new` から派生し、`core_iter_collectors.rs` の `collect_list_baseline` ケースで `prelude.collector.kind` を検証"
wbs = "3.1b F2"

[[api]]
module = "Collector"
name = "trait.with_capacity"
effect = "effect {mem}"
stage = "beta"
rust_status = "implemented"
ocaml_status = "not_available"
notes = "F2: `VecCollector::with_capacity`/`StringCollector` で `collector.effect.mem_reservation` を発火、`collect-iterator-audit --section collectors` の `collector.effect.mem` カラムに反映"
wbs = "3.1b F2"

[[api]]
module = "Collector"
name = "trait.push"
effect = "effect {mut}"
stage = "beta"
rust_status = "implemented"
ocaml_status = "not_available"
notes = "F2: `VecCollector::push` や `MapCollector::push` で `CollectError` を返し、`core_iter_collectors.rs` のエラースナップショットへ診断を転写"
wbs = "3.1b F2"

[[api]]
module = "Collector"
name = "trait.reserve"
effect = "effect {mut, mem}"
stage = "beta"
rust_status = "implemented"
ocaml_status = "not_available"
notes = "F2: `VecCollector::reserve` が `collector.effect.reserve` を記録し、`CollectError::CapacityOverflow` を `AuditEnvelope.metadata.collector.*` へ残す"
wbs = "3.1b F2"

[[api]]
module = "Collector"
name = "trait.finish"
effect = "effect {mem}"
stage = "beta"
rust_status = "implemented"
ocaml_status = "not_available"
notes = "F2: 各 Collector が `finish` 時に `CollectOutcome::audit()` を呼び、`reports/iterator-collector-summary.md` へ Stage/Efffect を出力する計画"
wbs = "3.1b F2"

[[api]]
module = "Collector"
name = "trait.into_inner"
effect = "@pure"
stage = "beta"
rust_status = "implemented"
ocaml_status = "not_available"
notes = "F2: `ListCollector` と `SetCollector` が `into_inner` で永続コレクションを返し、`collector.stage.actual` を `stable` に固定する"
wbs = "3.1b F2"

[[api]]
module = "Collector"
name = "ListCollector.new"
effect = "@pure"
stage = "stable"
rust_status = "implemented"
ocaml_status = "not_available"
notes = "F2-1: `collect_list_baseline` で `prelude.collector.kind=list`/`collector.effect.* = []` の `Stage=stable` を確定し、`collect-iterator-audit --section collectors --case list` の KPI 出力で `collector.effect.mem=0` を検証済み"
wbs = "3.1b F2"

[[api]]
module = "Collector"
name = "VecCollector.with_capacity"
effect = "effect {mem}"
stage = "beta"
rust_status = "implemented"
ocaml_status = "not_available"
notes = "F2-1: `with_capacity`/`VecCollector::reserve` で `collector.effect.mem_reservation` を記録し、`collect-iterator-audit --section collectors --case vec` の KPI に `collector.effect.mem>0` を反映しつつ `collector.effect.reserve` が監査出力に載ることを確認"
wbs = "3.1b F2"

[[api]]
module = "Collector"
name = "VecCollector.push"
effect = "effect {mut}"
stage = "beta"
rust_status = "implemented"
ocaml_status = "not_available"
notes = "F2-1: `collect_vec_mem_reservation` snapshot で `collector.effect.mut`/`collector.effect.mem` を固定し、`collect-iterator-audit` KPI に `collector.effect.mem` を追加した段階で `rust_status=implemented` 判定"
wbs = "3.1b F2"

[[api]]
module = "Collector"
name = "MapCollector.stage_requirement"
effect = "@pure"
stage = "beta"
rust_status = "working"
ocaml_status = "typeck-only"
notes = "F2-2: `StageRequirement::AtLeast(\"beta\")` を `CollectOutcome::stage()` から `AuditEnvelope.metadata.collector.stage.required`/`actual` に転写し、`docs/spec/3-1-core-prelude-iteration.md:188-206` と `docs/spec/3-2-core-collections.md:75-120` に沿った `collector.stage.mismatch_rate` を `collect-iterator-audit` で検出"
wbs = "3.1b F2-2"

[[api]]
module = "Collector"
name = "MapCollector.push"
effect = "@pure"
stage = "beta"
rust_status = "working"
ocaml_status = "not_available"
notes = "F2-2: `collect_map_duplicate` で `CollectError::DuplicateKey` を確認し、`AuditEnvelope.metadata.collector.stage.required = \"beta\"` / `collector.stage.actual` を記録した上で `collect-iterator-audit --section collectors --case collector-duplicate` へ `collector.effect.mem_reservation` と `collector.error.key` を送る"
wbs = "3.1b F2-2"

[[api]]
module = "Collector"
name = "CollectError::DuplicateKey"
effect = "@pure"
stage = "beta"
rust_status = "working"
ocaml_status = "typeck-only"
notes = "F2-2: 重複キー検出で `AuditEnvelope.metadata.collector.error.kind = \"duplicate_key\"`、`error.key` に `format!(\"{:?}\", key)`、`Diagnostic.extensions[\"prelude.collector.error_key\"]` を書き込み、`docs/spec/3-2-core-collections.md:200-235` のメトリクス要件と整合させる"
wbs = "3.1b F2-2"

[[api]]
module = "Collector"
name = "SetCollector.push"
effect = "@pure"
stage = "stable"
rust_status = "working"
ocaml_status = "not_available"
notes = "F2-2: `SetCollector` が `StageRequirement::Exact(\"stable\")` を守る `collect_set_stage` ケースで `collector.stage.actual = \"stable\"` を `Diagnostic.extensions[\"prelude.collector.stage\"]` と `AuditEnvelope.metadata.collector.stage.actual` に書き込み、`collector.effect.finish` まで `collector.stage.audit_pass_rate = 1.0` を維持"
wbs = "3.1b F2-2"

[[api]]
module = "Collector"
name = "SetCollector.stage_requirement"
effect = "@pure"
stage = "stable"
rust_status = "working"
ocaml_status = "typeck-only"
notes = "F2-2: `StageRequirement::Exact(\"stable\")` を `collector.stage.required`/`actual` 双方へ定義し、`collect-iterator-audit` の `collector.stage.mismatch_rate` が 0 になることを `reports/iterator-collector-summary.md` で検証"
wbs = "3.1b F2-2"

[[api]]
module = "Collector"
name = "StringCollector.finish"
effect = "effect {mem}"
stage = "beta"
rust_status = "working"
ocaml_status = "not_available"
notes = "F2-3: `StringError::InvalidEncoding` を `CollectError` で再利用し、`collect_string_invalid` の `collector.error.invalid_encoding`/`collector.effect.mem` を `reports/iterator-collector-summary.md` で監視"
wbs = "3.1b F2-3"

[[api]]
module = "Collector"
name = "TableCollector.push"
effect = "effect {mut}"
stage = "beta"
rust_status = "implemented"
ocaml_status = "not_available"
notes = "F2: `collect_table_baseline`/`collect_table_duplicate` スナップショットで挿入順保持と `CollectError::DuplicateKey` の `collector.effect.mut` 出力を固定し、`collector.effect.*` の auditor 連携準備を完了したため `Rust` 実装済み。"
wbs = "3.1b F2"
