# W4 診断ケースマトリクス

W4 では `scripts/poc_dualwrite_compare.sh --mode diag` と `scripts/dualwrite_summary_report.py --diag-table` を組み合わせて、カテゴリ別に代表ケースを回しながら OCaml/Rust フロントエンドの診断互換性を確認する。本マトリクスではカテゴリごとに最低 3 ケース（parser recover は 5 ケース）を登録し、入力種別・期待する拡張キー／メトリクス・準備状況を横断管理する。`Status` が Ready のケースは `docs/plans/rust-migration/appendix/w4-diagnostic-cases.txt` から自動読み込みできる。

- *2027-11-12 更新*: `compiler/ocaml/tests/test_cli_diagnostics.ml`, `streaming_runner_tests.ml`, `test_cli_callconv_snapshot.ml`, `test_ffi_contract.ml`, `docs/plans/bootstrap-roadmap/2-5-proposals/DIAG-002-proposal.md` など W4 Step2 で指定された参照元を横断し、各ケースへ「どのテスト／計画書を根拠にするか」「CLI/LSP でどのフラグを使うか」を明記した。これにより `poc_dualwrite_compare.sh --mode diag` と LSP フィクスチャの両方で同一入力・同一監査キーを辿れる。同日実行した `reports/dual-write/front-end/w4-diagnostics/20271112-w4-diag-m1/summary.md` では 21 ケースすべて `gating=false` となり、Rust 側診断に schema 情報が欠落している（`parser_audit=0.0`）こと、およびケース固有フラグが CLI へ伝播していないことが確認された。
- *2028-01-15 更新*: Run ID `20280115-w4-diag-refresh` を `scripts/poc_dualwrite_compare.sh --mode diag` で実行し、`reports/dual-write/front-end/w4-diagnostics/20280115-w4-diag-refresh/summary.md` を取得。Streaming 系は CLI フラグが適用され `runconfig.extensions.stream.enabled=true` になったが、`collect-iterator-audit-metrics.py` が `parser.stream_extension_field_coverage < 1.0` / `parser.expected_summary_presence < 1.0` を継続検出し、`reports/dual-write/front-end/w4-diagnostics/20280115-w4-diag-refresh/stream_pending_resume/parser-metrics.ocaml.err.log` と `reports/dual-write/front-end/w4-diagnostics/20280115-w4-diag-refresh/stream_backpressure_hint/parser-metrics.rust.err.log` に同指標が残った。Type/Effect 系は OCaml 側が `diagnostics.ocaml.json` に 1 件ずつ記録されるようになった一方、Rust 側 `diagnostics.rust.json` は空（`reports/dual-write/front-end/w4-diagnostics/20280115-w4-diag-refresh/type_condition_bool/diagnostics.rust.json`）のままで `summary.json` でも `rust_diag_count=0`。`reports/dual-write/front-end/w4-diagnostics/README.md` のケース表は `--diag-table` で更新済み。
- *2028-01-15 夕方更新*: 同 Run ID を再実行した結果、Streaming 系の `parser.stream_extension_field_coverage`／`parser.expected_summary_presence` は両フロントエンドで 1.0 まで回復し、Type/Effect 系も Rust 側 `diagnostics.rust.json` に parser 由来の recover 記録・`expected.alternatives` が出力されるようになった。`diagnostic.audit_presence_rate`／`parser.runconfig_switch_coverage` も ✅ になり、残るゲートは `lexer.identifier_profile_unicode`（lex プロファイルが `strict_json` のため監視モード）だけである。
- *2028-02-09 再測定*: Run `20280209-w4-diag-recover-r2` を全ケースで実行し、parser recover 5 ケースのうち `recover_lambda_body` で `gating/schema/metrics` が初めて全て ✅ になった。`summary.md` では `diag_match=✅` かつ OCaml/Rust の診断件数が 1 件ずつで一致し（`reports/dual-write/front-end/w4-diagnostics/20280209-w4-diag-recover-r2/summary.md:15`）、`parser-metrics.rust.json` も `parser.expected_summary_presence=1.0` と `parser_expected_ratio=1.0` を記録した（同 `.../parser-metrics.rust.json:22-44,571-587`）。
- *2028-02-10 再測定*: Recover ケースを `recover_else_without_if` / `recover_lambda_body` に絞り、`scripts/poc_dualwrite_compare.sh --mode diag --emit-expected-tokens expected_tokens --run-id 20280210-w4-diag-recover-else-r4` を実行。`recover_else_without_if` の `expected_tokens.diff.json` が `match=true`・`ocaml_count=rust_count=27` となり、`collect-iterator-audit-metrics.py --section parser` でも `diag_counts=1/1` / `parser.expected_summary_presence=1.0` を確認した（`reports/dual-write/front-end/w4-diagnostics/20280210-w4-diag-recover-else-r4/recover_else_without_if/`）。これにより DIAG-RUST-01 の残タスクは Streaming ケースのみとなる。
- *2025-05-14 更新*: Streaming 系の `expected_tokens` 取得を恒常化するため、`parser_expectation` に Collector を追加して `expected.alternatives` を維持しつつ placeholder を挿入、Rust CLI も `token` / `hint` キーで同一スキーマを出力するよう揃えた。`scripts/poc_dualwrite_compare.sh` は `stream_*` ケースで常に `expected_tokens/<case>.{ocaml,rust,diff}.json` を生成するため、以降のランでは追加フラグ無しで `ExpectedTokenCollector.streaming` メトリクスを読み出せる。
- *2029-05-31 更新*: Run `20290531-w4-diag-streaming-r16` では OCaml 側 `core_parse_streaming.ml:155` の `stream_config` 参照がスコープ外となり CLI 実行が `Unbound value` で停止、`diagnostics.ocaml.json` が空のため `expected_tokens.diff.json` が `ocaml_count=0` のまま固定されることを確認（`reports/dual-write/front-end/w4-diagnostics/20290531-w4-diag-streaming-r16/stream_backpressure_hint/ocaml.stderr.log`）。`stream_config` を `register_diagnostic` 先頭へ引き上げたうえで Run `20290531-w4-diag-streaming-r17` を再測定すると 3 ケースすべてで `diag_match=true`・`metrics_ok=true`・`expected_tokens_match=true` となり、`parser_expected_summary.json` による `collect-iterator-audit-metrics.py --section parser|streaming` も pass（`reports/dual-write/front-end/w4-diagnostics/20290531-w4-diag-streaming-r17/summary.md`）。ただし OCaml 診断の `extensions.parse` / `audit_metadata["parser.core.rule"]` が連想配列化されておらず `schema-validate.log` が `parser core metadata missing` を継続出力するため `gating=false` は解消されていない。
- *2029-06-01 更新*: `diagnostic_serialization.ml` に Streaming プレースホルダ（`extensions.parse.parser_id=*`, `audit_metadata["parser.core.rule"].*`）補完を追加し、Run `20290601-w4-diag-streaming-r21` で Streaming 3 ケースすべてが `schema_ok=true`・`gating=true` に到達。`scripts/validate-diagnostic-json.sh` でも `parser core metadata missing` が再発しないことを確認し、DIAG-RUST-05 を Close。
- *2029-05-23 更新*: Streaming（DIAG-RUST-05）ステップ 2 を再開するにあたり、`hook / OCaml backfill / CLI state + tests` の 3 作業単位へ分解し、本マトリクスにも対象ファイルと Run 条件を追記した。  
  - Rust hook: `compiler/rust/frontend/src/parser/mod.rs` と `frontend/src/diagnostic/recover.rs` に `checkpoint_end` フックと「1 checkpoint = 1 recover」制約を導入し、`scripts/poc_dualwrite_compare.sh --mode diag --case-filter '^stream_' --run-id 20290505-w4-diag-streaming-precheck` で計測する。  
  - OCaml backfill: `compiler/ocaml/src/core_parse_streaming.ml` / `streaming_runner_tests.ml` を更新して `stream_backpressure_hint` が常に 1 件の recover を emit することを保証し、`reports/dual-write/front-end/w4-diagnostics/20290505-w4-diag-streaming-precheck/stream_backpressure_hint/summary.json` を根拠にする。  
  - CLI/テスト: `compiler/rust/frontend/src/bin/poc_frontend.rs` に `StreamFlowState` を新設し、`compiler/rust/frontend/tests/streaming_metrics.rs` へ `expected_tokens_match` の恒常テストを追加。受入は Run `20290510-w4-diag-streaming-r12`（`ExpectedTokenCollector.streaming`, `parser.stream_extension_field_coverage`, `diag_counts` を 1.0 / match に揃える）で判断する。
- *2028-03-05 更新*: Type/Effect/Capability/FFI 系は `cases.txt` に共通フラグ `--experimental-effects --effect-stage beta --type-row-mode dual-write --emit-typeck-debug <dir>` を記載し、Rust 専用フラグ `#flags.rust: --emit-effects-metrics <dir>` を追加した。`scripts/poc_dualwrite_compare.sh` も OCaml 側 `effects-metrics.ocaml.json` を `effects/` へ複製するフォールバックを持つため、`20280305-w4-diag-effects`（`reports/dual-write/front-end/w4-diagnostics/20280305-w4-diag-effects/summary.md`）では type/effect 4 ケース中 3 ケースが `diag_match=true` / `metrics_ok=true` に復帰し、DIAG-RUST-06（triage 18-24 行）の再実行時は双方で `effects/effects-metrics.*.json` を保存できる。ただし `ffi_stage_messagebox` / `ffi_ownership_mismatch` / `ffi_async_dispatch` は Rust 側が parser recover 64/64/42 件で停止し、Stage 監査キーの比較に至っていない。  
- *2028-04-18 再測定*: `scripts/poc_dualwrite_compare.sh --mode diag --run-id 20280418-w4-diag-effects-r3 --cases docs/plans/rust-migration/appendix/w4-diagnostic-cases.txt --case-filter '^(type_|effect_|ffi_)' --emit-expected-tokens expected_tokens` を再実行し、`scripts/dualwrite_summary_report.py --diag-table` で README/本表を同期。`type_condition_bool|type_condition_literal_bool|effect_residual_leak|effect_stage_cli_override|ffi_stage_messagebox` は `diag_match=true` を達成した一方、`ffi_ownership_mismatch` / `ffi_async_dispatch` では `collect-iterator-audit-metrics.py --section effects --require-success` が `missing_keys=["effect.stage.required","effect.stage.actual","bridge.stage.required_capabilities","bridge.stage.actual_capabilities"]` を出力し、`effects-metrics.rust.err.log`（例: `reports/dual-write/front-end/w4-diagnostics/20280418-w4-diag-effects-r3/ffi_ownership_mismatch/effects-metrics.rust.err.log`）に保存された。原因は `compiler/rust/frontend/src/bin/poc_frontend.rs:835-858` が `effect.stage.*` を空配列のままシリアライズし、`build_audit_metadata`（同 1010-1074 行）も `bridge.stage.*` を注入していない点にあり、修正計画を `docs/plans/rust-migration/1-2-diagnostic-compatibility.md#ffi-stage-監査diag-rust-06` と `docs/plans/bootstrap-roadmap/2-7-deferred-remediation.md#TODO:-diag-rust-06` へ集約した。
- *2028-03-05 実行*: Run `20280305-w4-diag-effects` を `docs/plans/rust-migration/appendix/w4-diagnostic-cases.txt` の `type_|effect_|ffi_` ケースに限定して実施し、7 ケースすべてで `schema_ok=true` / `metrics_ok=true` を確認した（`reports/dual-write/front-end/w4-diagnostics/20280305-w4-diag-effects/summary.md`）。`ffi_async_dispatch` だけは OCaml 側診断が発生せず `diag_match=false` のままだが、`effects/effects-metrics.(ocaml|rust).json` は全ケースで揃い、以降のトリアージでは `diag_match` のみを `DIAG-RUST-06` で追跡する。
- *2028-04-18 更新*: `scripts/poc_dualwrite_compare.sh --mode diag` に `force_type_effect_flags` を追加し、`type_*` / `effect_*` / `ffi_*` エントリは `--experimental-effects --effect-stage beta --type-row-mode dual-write --emit-typeck-debug <case>/typeck`（共通）と `--emit-effects-metrics <case>/effects`（Rust 専用）を自動付与する。生成された `typeck/typeck-debug.{ocaml,rust}.json` と `effects/effects-metrics.{ocaml,rust}.json` を成果物必須セットとして扱い、`collect-iterator-audit-metrics.py --section effects --section parser` の `effect_scope.audit_presence` / `parser.expected_summary_presence` を `summary.json.metrics_ok` へ取り込む。
- *2028-04-18 実行*: Run `20280418-w4-diag-effects-r3`（`reports/dual-write/front-end/w4-diagnostics/20280418-w4-diag-effects-r3/summary.md`）で新フラグを適用し、7 ケース中 5 ケースが `diag_match=true` / `metrics_ok=true` を達成。`ffi_ownership_mismatch` と `ffi_async_dispatch` は Rust 側の Stage 監査キー欠落により `metrics_ok=false` のままなので、`docs/plans/bootstrap-roadmap/2-7-deferred-remediation.md#TODO:-diag-rust-06` に Run ID と `effects-metrics.rust.err.log` を登録した。
- *2028-03-18 再測定*: Streaming 系 3 ケースのみを `scripts/poc_dualwrite_compare.sh --mode diag --run-id 20280318-w4-diag-streaming-r11 --emit-expected-tokens expected_tokens/stream` で再実行。`parser_expected_summary_presence` / `parser.stream_extension_field_coverage` は 1.0 を維持したが、`diag_match=false`（`diag_counts.ocaml/rust = 1/11, 1/4, 0/5`）と `expected_tokens_match=false`（OCaml 側 0 or 27 件 vs Rust 側 1 件）が継続し、`expected_tokens/stream.diff.json` に差分が保存された。`docs/plans/bootstrap-roadmap/2-7-deferred-remediation.md#TODO:-DIAG-RUST-05` へ Run ID とケース別メモを転記済み。

## Parser Recover（5 cases）
| Case ID | Source（テスト/資料） | Input / CLI | Status | Metrics / Extensions | Notes |
| --- | --- | --- | --- | --- | --- |
| `recover_missing_semicolon` | `compiler/ocaml/tests/test_cli_diagnostics.ml`（recover 快復スナップショット） / `docs/plans/bootstrap-roadmap/2-5-review-log.md`（Step3 `parser_recover_tests.ml` 記録） | inline（`cases.txt`） / diag デフォルト（`--packrat --format json --json-mode compact`） | Ready（20271107-w4-new: diag✅ / metrics⚠️ `parser.stream_extension_field_coverage`） | `extensions.recover.expected_tokens`, `parser.expected_summary` | 2 行連続の `let` 文でセミコロンを欠落させ、同期トークン復旧を比較する |
| `recover_unclosed_block` | 同上 + `docs/plans/bootstrap-roadmap/2-5-proposals/ERR-002-proposal.md`（FixIt 要件） | inline（`cases.txt`） / diag デフォルト | Ready（20271107-w4-new: diag✅ / metrics⚠️） | `extensions.recover.sync_tokens`, `span_trace` | `{ ...` でブロックを閉じず EOF へ到達。Packrat `span_trace` と sync token の一致を確認 |
| `recover_else_without_if` | `compiler/ocaml/tests/golden/diagnostics/parser/expected-summary.json.golden`（`parse.expected`） | inline（`cases.txt`） / diag デフォルト | Ready + Pass（20280210-w4-diag-recover-else-r4: diag✅/metrics✅） | `parser.expected.{token,alternatives}`、`expected_tokens.diff.json` | `expected_tokens` 27 件（`if/loop/identifier/...`）が OCaml/Rust 一致し、`collect-iterator-audit-metrics.py --section parser` も `diag_counts.ocaml == diag_counts.rust == 1` / `parser.expected_summary_presence=1.0` を記録（`reports/dual-write/front-end/w4-diagnostics/20280210-w4-diag-recover-else-r4/recover_else_without_if/{summary.md,parser-metrics.rust.json:22-44}`）。 |
| `recover_missing_tuple_comma` | `compiler/ocaml/tests/test_parser.ml`（tuple 期待値） | inline（`cases.txt`） / diag デフォルト | Ready（20271107-w4-new: diag✅ / metrics⚠️） | `extensions.recover.notes`, `diagnostic.v2.codes` | タプル内カンマ欠落で複数候補の列挙順を比較 |
| `recover_lambda_body` | `compiler/ocaml/tests/test_cli_diagnostics.ml`（lambda recover note） | inline（`cases.txt`） / diag デフォルト | Ready + Pass（20280209-w4-diag-recover-r2: diag✅/metrics✅） | `extensions.recover.has_fixits`, `parser.stream.backpressure`, `diag_counts` | Rust 側 `parse.expected` が 1 件に揃い `diag_counts.ocaml == diag_counts.rust == 1`（`reports/dual-write/front-end/w4-diagnostics/20280209-w4-diag-recover-r2/summary.md:15`）。`collect-iterator-audit-metrics.py --section parser` も `parser.expected_summary_presence=1.0` と `parser_expected_ratio=1.0` を報告（`reports/dual-write/front-end/w4-diagnostics/20280209-w4-diag-recover-r2/recover_lambda_body/parser-metrics.rust.json:22-44,571-587`） |

*2027-11-12 実行結果*: `reports/dual-write/front-end/w4-diagnostics/20271112-w4-diag-m1/` では 5 ケース中 3 ケース（missing_semicolon / missing_tuple_comma / unclosed_block）が `diag_match=✅` だが Rust 側 `parser_audit` は 0.0 のまま（例: `reports/dual-write/front-end/w4-diagnostics/20271112-w4-diag-m1/recover_missing_semicolon/parser-metrics.rust.err.log`）。`recover_else_without_if` は `reports/dual-write/front-end/w4-diagnostics/20271112-w4-diag-m1/recover_else_without_if/diagnostics.rust.json` が空、`recover_lambda_body` は Rust Recover が 2 件のままで `gating=false`（`DIAG-RUST-05` 継続）。

*2028-Q2 フォローアップ*: `docs/plans/bootstrap-roadmap/2-7-deferred-remediation.md#TODO:-DIAG-RUST-01` に従い、`scripts/poc_dualwrite_compare.sh --mode diag --emit-expected-tokens <dir> --case-filter '^recover_(else_without_if|lambda_body)$'` を追加し、`expected_tokens.{ocaml,rust}.json` / `expected_tokens.diff.json` と `parser-metrics.*.json` を根拠に `diag_counts=1/1`・`parser.expected_summary_presence=1.0` を確認後、上記 2 ケースの `Status` を Ready + Pass に更新する。

## Streaming Meta（3 cases）
| Case ID | Source（テスト/資料） | Input / CLI | Status | Metrics / Extensions | Notes |
| --- | --- | --- | --- | --- | --- |
| `stream_pending_resume` | `compiler/ocaml/tests/streaming_runner_tests.ml::test_pending_resume_flow` | inline（`cases.txt`） / `--streaming --stream-resume-hint diag-w4 --stream-flow-policy auto --stream-demand-min-bytes 4` | 20280318-w4-diag-streaming-r11: schema✅/metrics✅/diag❌（`diag_counts`=1/11, `expected_tokens_match=false`） | `parser.stream.outcome_consistency`, `resume_hint` | `diagnostics.w4.stream_pending_resume` 入力を diag モードで再測定。`expected_tokens/stream.diff.json` に `fn` のみ出力される Rust 側と 0 件の OCaml 側の差分を保存 |
| `stream_backpressure_hint` | `streaming_runner_tests.ml::test_streaming_matches_batch` / `docs/guides/core-parse-streaming.md` §3 | inline（`cases.txt`） / `--streaming --stream-flow-policy auto --stream-flow-max-lag 8192` | 20280318-w4-diag-streaming-r11: schema✅/metrics✅/diag❌（`diag_counts`=0/5, `expected_tokens_match=false`） | `parser.stream.backpressure_sync`, `stream_meta.backpressure_events` | Flow Controller Auto で backpressure イベントと `resume_lineage` を比較。OCaml 側診断 0 件のままのため Rust recover 5 件を 1 件へ圧縮するタスクを `DIAG-RUST-05` で追跡 |
| `stream_checkpoint_drift` | `streaming_runner_tests.ml::test_checkpoint_restore`（補助メモ） | inline（`cases.txt`） / `--streaming --stream-checkpoint diag-w4` | 20280318-w4-diag-streaming-r11: schema✅/metrics✅/diag❌（`diag_counts`=1/4, `expected_tokens` 27 件 vs 1 件） | `parser.stream.demandhint_coverage`, `last_checkpoint` | checkpoint 位置ずれをトレースし、`expected_tokens/stream.diff.json` （27 件差）を元に Rust recover の重複解除を進める |

*2027-11-12 実行結果*: diag ハーネスが `#flags` を CLI へ適用していないため、3 ケースすべて `--streaming` なしで実行され `schema-validate.log` が `diagnostics[0].expected` 欠落で失敗し、`parser.stream_extension_field_coverage` が 0.0（例: `reports/dual-write/front-end/w4-diagnostics/20271112-w4-diag-m1/stream_pending_resume/schema-validate.log`）。`DIAG-RUST-05` にて `scripts/poc_dualwrite_compare.sh` 側で CLI フラグ注入を実装する。

## Type & Effect（3 cases）
| Case ID | Source（テスト/資料） | Input / CLI | Status | Metrics / Extensions | typeck-debug | effects-metrics | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `type_condition_bool` | `compiler/ocaml/tests/test_type_inference.ml` / `docs/spec/1-2-types-Inference.md`（条件型診断） | inline（`cases.txt`） / `--experimental-effects --effect-stage beta --type-row-mode dual-write --emit-typeck-debug <tmp>` | 20280602-w4-diag-type-condition-r2: schema✅/metrics✅/diag✅（`reports/dual-write/front-end/w4-diagnostics/20280602-w4-diag-type-condition-r2/type_condition_bool/summary.json`） | `effects.stage.*`, `diagnostic_code=TYPE_condition_bool` | `20280602-w4-diag-type-condition-r2 / typeck/typeck-debug.{ocaml,rust}.json` | `20280602-w4-diag-type-condition-r2 / effects/effects-metrics.{ocaml,rust}.json` | `--force-type-effect-flags` で Type 行は完了基準を満たし、`typeck_requirements.frontends.*.ok=true` を確認済み |
| `type_condition_literal_bool` | `compiler/ocaml/tests/test_type_errors.ml`（literal guard） | inline（`cases.txt`、`if 1 then ...`） / `--experimental-effects --effect-stage beta --type-row-mode dual-write --emit-typeck-debug <tmp>` | 20280602-w4-diag-type-condition-r2: schema✅/metrics✅/diag✅（`reports/dual-write/front-end/w4-diagnostics/20280602-w4-diag-type-condition-r2/type_condition_literal_bool/summary.json`） | `diagnostic_code=TYPE_condition_bool`, `recover.has_fixits` | `20280602-w4-diag-type-condition-r2 / typeck/typeck-debug.{ocaml,rust}.json` | `20280602-w4-diag-type-condition-r2 / effects/effects-metrics.{ocaml,rust}.json` | Rust の typed parameter 対応で `diag_match=true` を再現、Type 行の残タスクは解消済み |
| `effect_residual_leak` | `compiler/ocaml/tests/test_effect_residual.ml` / `docs/plans/bootstrap-roadmap/2-5-proposals/EFFECT-002-proposal.md` | inline（`cases.txt`、`effect ConsoleLog` snippet） / `--experimental-effects --effect-stage beta --type-row-mode dual-write --emit-typeck-debug <tmp>` | 20280601-w4-diag-type-effect-rust-typeck-r7: schema✅/metrics❌/diag❌（OCaml 1 件、Rust 2 件、`expected_tokens.diff.json` で `ocaml_count=1` vs `rust_count=0`） | `effects.residual`, `effect.required_capabilities` | `20280601-w4-diag-type-effect-rust-typeck-r7 / typeck/typeck-debug.rust.json`（Rust ✅） / `typeck-debug.ocaml.json`（❌: `typeck_debug_exists=false`） | `20280601-w4-diag-type-effect-rust-typeck-r7 / effects/effects-metrics.{ocaml,rust}.json` | `parser-metrics.rust.json` が `parser.expected_summary_presence=0.0` を記録。Rust parser recover を 1 件へ抑制し `expected_tokens` を復元するタスクが継続 |
| `effect_stage_cli_override` | `compiler/ocaml/tests/test_cli_callconv_snapshot.ml::callconv_windows_messagebox`（stage override） | inline（`cases.txt`、`@requires_capability(stage="beta")`) / `--experimental-effects --effect-stage beta --type-row-mode dual-write --emit-typeck-debug <tmp>` | 20280305-w4-diag-effects: metrics✅/diag❌（Rust 側 6 件、OCaml 1 件、`reports/dual-write/front-end/w4-diagnostics/20280305-w4-diag-effects/effect_stage_cli_override/summary.json`） | `effects.stage_trace`, `effect.stage.inputs.cli` | `20280418-w4-diag-effects-r3 / typeck/typeck-debug.{ocaml,rust}.json` | `20280418-w4-diag-effects-r3 / effects/effects-metrics.{ocaml,rust}.json` | Stage override ＋ Capability を再測定。`force_type_effect_flags` で CLI 条件と成果物を固定 |

*2027-11-12 実行結果*: OCaml 側 CLI が parser で終了しており `reports/dual-write/front-end/w4-diagnostics/20271112-w4-diag-m1/type_condition_bool/diagnostics.ocaml.json` などが空配列、Rust 側は recover 1 件（`reports/dual-write/front-end/w4-diagnostics/20271112-w4-diag-m1/type_condition_bool/diagnostics.rust.json`）のまま。`effect_*` ケースも CLI フラグ未適用のため lex error 群（5〜6 件）が記録され、`effects.*` メトリクスは計測不可。`DIAG-RUST-06` に沿って `--experimental-effects --type-row-mode dual-write --emit-typeck-debug <tmp>` を diag モードへ注入する。

## Capability Stage / FFI（3 cases）
| Case ID | Source（テスト/資料） | Input / CLI | Status | Metrics / Extensions | typeck-debug | effects-metrics | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ffi_stage_messagebox` | `compiler/ocaml/tests/test_cli_callconv_snapshot.ml::callconv_windows_messagebox` | file `examples/ffi/windows/messagebox.reml` / `--experimental-effects --effect-stage beta --type-row-mode dual-write --runtime-capabilities windows.messagebox --emit-typeck-debug <tmp>` | 20280601-w4-diag-type-effect-rust-typeck-r7: schema✅/metrics❌/diag❌（OCaml 1 件、Rust 43 件、`expected_tokens.ocaml_count=12` vs `rust_count=2`） | `bridge.stage.*`, `effect.stage.actual_capabilities` | `20280601-w4-diag-type-effect-rust-typeck-r7 / typeck/typeck-debug.rust.json`（Rust ✅） / `typeck-debug.ocaml.json`（❌） | `20280601-w4-diag-type-effect-rust-typeck-r7 / effects/effects-metrics.{ocaml,rust}.json` | Stage 監査キーは pass するが parser recover が 43 件残存し、`expected_tokens.diff.json` が埋まらない。`TypeEffectRecoverLimiter` 実装待ち |
| `ffi_ownership_mismatch` | `compiler/ocaml/tests/test_ffi_contract.ml::ownership_transfer` | file `examples/ffi/windows/ownership_transfer.reml` / `--experimental-effects --effect-stage beta --type-row-mode dual-write --runtime-capabilities windows.ffi --emit-typeck-debug <tmp>` | 20280601-w4-diag-type-effect-rust-typeck-r7: schema✅/metrics❌/diag❌（OCaml 1 件、Rust 21 件、`parser.expected_summary_presence=0.0476`） | `bridge.audit_pass_rate`, `ffi.contract.*` | `20280601-w4-diag-type-effect-rust-typeck-r7 / typeck/typeck-debug.rust.json`（Rust ✅） / `typeck-debug.ocaml.json`（❌） | `20280601-w4-diag-type-effect-rust-typeck-r7 / effects/effects-metrics.{ocaml,rust}.json` | Rust parser recover が `expected` キーを欠落した 20 件を生成し `collect-iterator` が `typeck_debug_match` を検知。`expected_tokens.diff.json`（5 vs 2 件）をゼロ化するタスクを継続 |
| `ffi_async_dispatch` | `compiler/ocaml/tests/test_ffi_contract.ml::ffi_dispatch_async`（W3 TODO） | file `examples/ffi/macos/ffi_dispatch_async.reml` / `--experimental-effects --effect-stage beta --type-row-mode dual-write --runtime-capabilities macos.dispatch_queue --emit-typeck-debug <tmp>` | 20280601-w4-diag-type-effect-rust-typeck-r7: schema✅/metrics❌/diag❌（OCaml 0 件、Rust 20 件、`expected_tokens.ocaml_count=0` vs `rust_count=2`） | `effect.stage.runtime_capabilities`, `ffi.async.bridge` | `20280601-w4-diag-type-effect-rust-typeck-r7 / typeck/typeck-debug.{ocaml,rust}.json`（OCaml/Rust 共にファイルは生成、OCaml 側 `parser-metrics` 欠落） | `20280601-w4-diag-type-effect-rust-typeck-r7 / effects/effects-metrics.{ocaml,rust}.json` | macOS dispatch queue で OCaml CLI が依然診断 0 件。`cases.txt` の CLI フラグ再確認と `parser-metrics.ocaml.json` 出力が TODO |


*2027-11-12 実行結果*: Rust 側は 42〜64 件の lex recover 診断を出力し（例: `reports/dual-write/front-end/w4-diagnostics/20271112-w4-diag-m1/ffi_stage_messagebox/diagnostics.rust.json`）、OCaml 側は `--runtime-capabilities` 未指定のため parser で停止。`bridge.*`/`effect.stage.*` メトリクスはいずれも計測できず `metrics_ok=false`。CLI フラグ伝播と Capability 監査 JSON の移植が `DIAG-RUST-06` の前提。

## CLI Config / RunConfig（3 cases）
| Case ID | Source（テスト/資料） | Input / CLI | Status | Metrics / Extensions | Notes |
| --- | --- | --- | --- | --- | --- |
| `cli_packrat_switch` | `compiler/ocaml/tests/run_config_tests.ml::packrat_switch` / `docs/plans/bootstrap-roadmap/2-5-proposals/DIAG-002-proposal.md`（監査必須フィールド） | file `examples/cli/emit_suite.reml` / `--packrat` と `--no-packrat` を順番に指定 | 20280430-w4-diag-cli-lsp: `parser.runconfig_switch_coverage=1.0`（Rust CLI が `extensions.config.path` を出力）／OCaml 診断 0 件で `diag_match=false` | `parser.runconfig.switch_coverage`, `runconfig.packrat`, `audit.*` | RunConfig 拡張は揃ったが、OCaml CLI 側が parser で終了するため diag 復旧は `DIAG-RUST-07` 管理 |
| `cli_trace_toggle` | `compiler/ocaml/tests/run_config_tests.ml::trace_options` | file `examples/cli/trace_sample.reml` / `--trace --no-merge-warnings` | 20280430-w4-diag-cli-lsp: `[TRACE]` ログ分離と RunConfig 拡張を確認／OCaml 診断欠落で `diag_match=false` | `parser.runconfig.extensions.lex`, `extensions.cli.trace_toggle`, `trace` | `reports/.../cli_trace_toggle/summary.json` に記録。OCaml CLI の diag 強制実行が残課題 |
| `cli_merge_warnings` | `compiler/ocaml/tests/test_cli_diagnostics.ml`（warning merge スナップショット） | file `examples/cli/type_error.reml` / `--no-merge-warnings --json-mode compact` | 20280430-w4-diag-cli-lsp: RunConfig 拡張は一致／OCaml 診断 0 件で `diag_match=false` | `parser.runconfig.extensions.recover`, `diagnostic.audit.*`, `extensions.config.*` | `reports/.../cli_merge_warnings/summary.json` 参照。`--no-merge-warnings` の diff は `lsp/cli_merge_warnings.diff` で追跡 |

*2027-11-12 実行結果*: `cli_packrat_switch` と `cli_merge_warnings` は OCaml 側診断が 0 件（`reports/dual-write/front-end/w4-diagnostics/20271112-w4-diag-m1/cli_packrat_switch/diagnostics.ocaml.json` 等が空）で `parser.runconfig_switch_coverage` が測定できず、Rust 側は 1〜4 件の recover 診断を出力。`cli_trace_toggle` も `parser_audit=0.0` のまま。  
*2028-04-30 再実行計画*: `scripts/poc_dualwrite_compare.sh --mode diag --run-id 20280430-w4-diag-cli-lsp --cases docs/plans/rust-migration/appendix/w4-diagnostic-cases.txt --case-filter '^(cli_)' --emit-expected-tokens expected_tokens/cli` を実行し、`parser.runconfig_switch_coverage` / `extensions.config.path` / `extensions.cli.trace_toggle` の一致を確認する。Rust CLI の RunConfig 実装は `docs/plans/rust-migration/1-2-diagnostic-compatibility.md#1-2-17-cli-lsp-runconfig-整合diag-rust-07` を参照。

## LSP RPC（3 cases）
| Case ID | Source（テスト/資料） | Input / CLI | Status | Metrics / Extensions | Notes |
| --- | --- | --- | --- | --- | --- |
| `lsp_hover_internal_error` | `tooling/lsp/tests/client_compat/fixtures/diagnostic-v2-hover-internal.json` / `docs/spec/3-6-core-diagnostics-audit.md`（LSP 監査） | file `examples/cli/type_error.reml` / CLI: `--format json --emit-parse-debug <tmp>` / LSP: `npm run ci --prefix tooling/lsp/tests/client_compat -- diag-w4 <run>` | 20280430-w4-diag-cli-lsp: `reports/.../lsp/lsp_hover_internal_error.diff` 生成済み（`extensions.config.path` 一致）／OCaml CLI 診断 0 件で `diag_match=false` | `domain=lsp`, `audit_metadata.lsp.*`, `extensions.config.*` | Diff 固定化済み。`diag_match` 復帰は CLI 側診断を増やした後に再測定 |
| `lsp_diagnostic_stream` | `tooling/lsp/tests/client_compat/fixtures/diagnostic-v2-stream.json` / `streaming_runner_tests` | file `examples/cli/trace_sample.reml` / CLI: `--streaming --stream-resume-hint diag-w4` / LSP: `npm run ci ... -- diag-w4 <run>` | 20280430-w4-diag-cli-lsp: `lsp/lsp_diagnostic_stream.diff` 生成済み／`diag_match=false`（OCaml CLI 診断 0 件） | `stream_meta.*`, `parser.stream.*`, `audit_metadata.channel` | `parser.runconfig_switch_coverage` は 1.0。LSP diff を維持しつつ CLI 側診断復旧が必要 |
| `lsp_workspace_config` | `tooling/lsp/tests/client_compat/fixtures/diagnostic-v2-workspace-config.json` / `docs/plans/bootstrap-roadmap/2-5-proposals/DIAG-002-proposal.md` | file `examples/cli/emit_suite.reml` / CLI: `--config workspace.json`（cases 付属） | 20280430-w4-diag-cli-lsp: `lsp/lsp_workspace_config.diff` 生成済み／OCaml CLI 診断 0 件で `diag_match=false` | `extensions.config.*`, `audit_metadata.cli.change_set`, `audit.timestamp` | diff 生成・RunConfig 拡張は整合。`diag_match` 復帰は OCaml CLI 側診断取得が前提 |

*2027-11-12 実行結果*: CLI 側は `w4-diagnostic-cases.txt` のフラグ未適用で LSP ケースと同一設定にならず、OCaml 診断が 0〜1 件、Rust 側は 1〜4 件の recover 診断に留まる。`npm run ci --prefix tooling/lsp/tests/client_compat` のログは baseline 通過済みだが、dual-write ランでは CLI/LSP の入出力が同期していないため `reports/dual-write/front-end/w4-diagnostics/20271112-w4-diag-m1/lsp_*` ディレクトリには diff が生成されていない。  
*2028-04-30 更新*: `npm run ci --prefix tooling/lsp/tests/client_compat -- diag-w4 20280430-w4-diag-cli-lsp` を実行し、CLI 側成果物（`diagnostics.rust.json`）と LSP フィクスチャの diff を `reports/dual-write/front-end/w4-diagnostics/20280430-w4-diag-cli-lsp/lsp/<case>.diff` に保存。`parser.runconfig_switch_coverage=1.0` は確認できたが、OCaml CLI 診断が 0 件のため `diag_match` は未復帰。`docs/plans/bootstrap-roadmap/2-7-deferred-remediation.md#TODO:-diag-rust-07` で継続追跡する。
> 実行時は `docs/plans/rust-migration/appendix/w4-diagnostic-cases.txt` に記載した入力を `scripts/poc_dualwrite_compare.sh --mode diag` へ渡し、ストリーミング／効果系ケースでは CLI フラグ（`--streaming`, `--stream-resume-hint`, `--experimental-effects`, `--effect-stage beta` など）を明示的に付与する。LSP ケースは `npm run ci --prefix tooling/lsp/tests/client_compat` と同期し、同一入力ファイルを `cases.txt` で参照する。

### TODO リンク
- **DIAG-RUST-05** — ストリーミング復旧ケース（`stream_pending_resume` ほか）を作成し、`parser.stream.*` 系メトリクスが Rust/OCaml で比較できるようにする。
- **DIAG-RUST-06** — 効果・Capability Stage・FFI 関連の入力を整理し、`effects.*` / `bridge.*` 拡張の diff を確認できるようにする。
- **DIAG-RUST-07** — CLI RunConfig/LSP RPC 用のケースを `cases.txt` と LSP フィクスチャの双方に登録し、`parser.runconfig.*` と LSP ログが食い違わないようにする。
- *2028-04-18 更新*: Run `20280418-w4-diag-effects-r3`（`reports/dual-write/front-end/w4-diagnostics/20280418-w4-diag-effects-r3/summary.md`）で Type/Effect/FFI ケースを再測定したが、`effect_residual_leak` の `diag_counts` が `1 vs 5`、`type_condition_literal_bool` は Rust 側診断 0 件、`ffi_ownership_mismatch` / `ffi_async_dispatch` は `effects-metrics.rust.err.log` に `missing_keys` が残り `effect.stage.*` が未出力のまま。Stage 監査を Rust へ実装しない限り DIAG-RUST-06 をクローズできない。
- *2028-06-01 更新*: Stage/Audit 拡張後に Run `20280601-w4-diag-type-effect-rust-typeck-r7` を取得したところ、`effect_residual_leak/summary.json` は `ocaml_diag_count=1` / `rust_diag_count=2`・`expected_tokens.ocaml_count=1` / `rust_count=0` を継続、`ffi_stage_messagebox` / `ffi_ownership_mismatch` も Rust 側 recover が 21〜43 件（`parser-metrics.rust.json` で `parser.expected_summary_presence < 0.05`）となり `expected_tokens.diff.json`（例: `ffi_ownership_mismatch` で `5 vs 2 件`）が埋まらない。`ffi_async_dispatch` は OCaml 診断 0 件のまま `metrics_ok=false`。Rust parser での `expected` 欠落と OCaml `typeck/typeck-debug.ocaml.json` の未生成が DIAG-RUST-06 の主因として残っている。
- *2028-04-30 更新*: `20280430-w4-diag-cli-lsp` では LSP `diagnostic-v2-stream` だけが pass し、`cli_packrat_switch` / `cli_merge_warnings` は OCaml 側診断 0 件で `diag_match=false`。`parser.runconfig_switch_coverage` も Rust のみ 1.0 で CLI RunConfig の同期未完了が確認された。
- *2029-04-15 更新*: Streaming 再測定 `20290415-w4-diag-streaming-recheck2` では `stream_pending_resume` / `stream_checkpoint_drift` が `diag_match=true` まで戻ったものの、`expected_tokens` が `27 vs 1` のまま `metrics_ok=false` に逆戻り。`stream_backpressure_hint` も OCaml 側診断 0 件で `diag_counts` が揃わず、`ExpectedTokenCollector.streaming` の pass が再度ブロックされている。
