# JSON スキーマ一覧

このディレクトリには CLI/診断/監査向けの JSON スキーマを配置する。

| ファイル | 用途 |
| --- | --- |
| `diagnostic.schema.json` | 診断 JSON フォーマット |
| `remlc-metrics.schema.json` | CLI メトリクス出力 |
| `plugin-bundle-registration.schema.json` | `reml plugin install --output json` の出力 |
